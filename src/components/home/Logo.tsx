"use client"
import { Target } from 'lucide-react'
import { motion } from 'motion/react'

const Logo = () => {
    return (
        <motion.div>
            <Target /> 
        </motion.div>
    )
}

export default Logo